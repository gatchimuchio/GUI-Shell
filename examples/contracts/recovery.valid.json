{
  "recovery_id": "recover-1",
  "class": "permission_denied",
  "severity": "warning",
  "user_visible_message": "Permission is required before this action can run.",
  "safe_to_retry": true,
  "steps": [
    "Review the pending approval.",
    "Retry after approval is granted."
  ],
  "requires_user_action": true
}
