# macos installer

Placeholder for product installer work.

Goal:

```text
Installer complete -> app launch -> experience start
```

Normal users must not be required to manually use terminal, WSL, npm, Git, port setup, or runtime root discovery.
